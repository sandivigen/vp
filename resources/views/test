@extends('layout.main')

@section('title', $heading)

{{-- if user not exists--}}
@if($user_exists == 1)

    <?php
        // Get users list
        $user = array();
        foreach($users as $key=>$value){
            $user[$value->id] = $value->name;
        }

        // Get article list
        $article_list = array();
        foreach($articles as $key=>$value){
            $article_list[$value->id] = $value->title;
        }

    //Debugbar::info($article_list);
    ?>

    @section('content')

            <div class="row">

                @if($comments)
                    <section class="content">
                        <div class="col-md-12">
                            <div class="panel panel-default">
                                <div class="panel-body">
                                    <?php
                                    // amount of comments
                                    $count_comment = 0;
                                    foreach($comments as $comment) {
                                        if(Auth::user()->id == $comment->user_id)
                                            $count_comment += 1;
                                    }
                                    ?>
                                    <div>
                                        <h3>Мои комментарии ({{ $count_comment }})</h3>
                                    </div>
                                    <div class="table-container">
                                        <table class="table table-filter">
                                            <tbody>
                                                @foreach($comments as $comment)
                                                    {{--{{ Debugbar::info($comment) }}--}}
                                                    @if(Auth::user()->id == $comment->user_id)
                                                        <tr data-status="pagado">
                                                            <td>
                                                                <div class="media">
                                                                    <a href="#" class="pull-left">
                                                                        {{--<img src="/images/{{ $article->thumbnail }}" class="media-photo">--}}
                                                                    </a>
                                                                    <div class="media-body">
                                                                        <span class="media-meta pull-right">{{ $comment->created_at->format('d M Y') }}</span>
                                                                        <h4 class="title">
                                                                            <a href="/articles/{{ $comment->category_item_id }}"> {{ $article_list[$comment->category_item_id] }}</a>
                                                                            <span class="pull-right pagado">Like (0)</span>
                                                                        </h4>
                                                                        <p class="summary">{{ $comment->comment_text }}</p>
                                                                    </div>
                                                                </div>
                                                            </td>
                                                        </tr>
                                                    @endif
                                                @endforeach
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </section>
                @else
                    <p>No Comment Found</p>
                @endif


                @if($articles)
                    <section class="content">
                        <div class="col-md-12">
                            <div class="panel panel-default">
                                <div class="panel-body">
                                    <?php
                                        // amount of comments
                                        $count_articles = 0;
                                        foreach($articles as $article) {
                                            if(Auth::user()->id == $article->user_id)
                                                $count_articles += 1;
                                        }
                                    ?>
                                    <div>
                                        <h3>Мои статьи ({{ $count_articles }})</h3>
                                    </div>

                                    <div class="table-container">
                                        <table class="table table-filter">
                                            <tbody>
                                                @foreach($articles as $article)
                                                    {{--                                        {{ Debugbar::info($article) }}--}}
                                                    @if(Auth::user()->id == $article->user_id)
                                                        <tr data-status="pagado">
                                                            <td>
                                                                <div class="media">
                                                                    <a href="#" class="pull-left">
                                                                        <img src="/images/{{ $article->thumbnail }}" class="media-photo">
                                                                    </a>
                                                                    <div class="media-body">
                                                                        {{--<span class="media-meta pull-right">Febrero 13, 2016</span>--}}
                                                                        <span class="media-meta pull-right">{{ $article->created_at->format('d M Y') }}</span>

                                                                        <h4 class="title">
                                                                            <a href="/articles/{{ $article->id }}"> {{ $article->title }}</a>
                                                                            @if(!Auth::guest())
                                                                                @if(Auth::user()->id == $article->user_id)
                                                                                    <a href="comments/{{ $article->id }}/edit" class="btn btn-default btn-xs"><em class="fa fa-pencil"></em></a>
                                                                                    {!! Form::open(['method' => 'DELETE', 'route' => ['articles.destroy', $article->id], 'class' => 'form-delete-userpage']) !!}
                                                                                    <button type="submit" class="btn btn-danger btn-xs"><em class="fa fa-trash"></em></button>
                                                                                    {!! Form::close() !!}
                                                                                @endif
                                                                            @endif
                                                                            <span class="media-meta pull-right">{{ $article->category }}</span>
                                                                        </h4>
                                                                        <p class="summary">{{ str_limit($article->text, $limit = 300, '...') }} <a href="/articles/{{ $article->id }}">read more</a></p>
                                                                    </div>
                                                                </div>
                                                            </td>
                                                            <td align="center" class="comment-btn-block">
                                                                <br>
                                                            </td>
                                                        </tr>
                                                    @endif
                                                @endforeach
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </section>
                @else
                    <p>No Article Found</p>
                @endif

            </div>
        </div>

    @stop

@endif